# @commerce/adapter-bigcommerce

`CommerceAdapter` + `MerchantAdapter` against BigCommerce's Storefront/
Catalog and Admin V3 APIs. Handed to the team as a separate follow-up
prompt once the Shopify adapter is solid.
