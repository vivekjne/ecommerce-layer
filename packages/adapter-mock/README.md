# @commerce/adapter-mock

In-memory `CommerceAdapter` + `MerchantAdapter`, seeded from a JSON fixture
of ~25 products. Built in Step 3. This is the demo fallback and the
reference implementation the contract tests were written against.
