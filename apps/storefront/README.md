# apps/storefront

React Router (framework mode) storefront with an AI shopping chat: Vercel
AI SDK `streamText` (Anthropic provider), `STOREFRONT_SYSTEM_PROMPT`, and
the shared shopper tools. Write-tool results render as `ConfirmCard`
before any adapter call executes. Built in Step 4.
